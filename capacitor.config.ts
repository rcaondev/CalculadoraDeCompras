import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'minhaCalculadoraDeCompras',
  webDir: 'dist'
};

export default config;
